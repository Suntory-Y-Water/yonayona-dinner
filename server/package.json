{
  "name": "yonayona-dinner-server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "AGENT=1 bun test --bail=2",
    "dev": "bun run --hot src/index.ts",
    "dev:wrangler": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@date-fns/tz": "^1.4.1",
    "@hono/zod-validator": "^0.7.4",
    "date-fns": "^4.1.0",
    "date-fns-tz": "^3.2.0",
    "hono": "^4.10.1",
    "yonayona-dinner-shared": "workspace:*",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.43.0"
  }
}
